syntax = "proto3";

message Message {
    string text = 1;
    int32 chatID = 2;
    optional int64 time = 3;
}

message Empty {

}

service MessageService {
    // From server to bot
    rpc Send (Message) returns (Empty) {}
    // From api to bot
    rpc Recieve (Message) returns (Empty) {}
}
